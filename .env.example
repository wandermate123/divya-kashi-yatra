# ── Database (PostgreSQL) ─────────────────────────────────────────
# Vercel: Project → Settings → Environment Variables → add DATABASE_URL for Production
#   (and Preview if you use it). Redeploy after saving — env changes don’t apply to old deploys.
#
# Supabase: Project Settings → Database → Connection string → URI → Transaction pooler (port 6543).
# Add query params if missing: ?pgbouncer=true&sslmode=require&connect_timeout=30
# Serverless-friendly: append &connection_limit=1
#
# Example (replace PROJECT_REF, REGION, PASSWORD):
#   postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require&connect_timeout=30&connection_limit=1
#
# URL-encode special characters in PASSWORD (e.g. @ → %40, # → %23). No spaces or line breaks.
#
# Migrations: run `npx prisma migrate deploy` locally using Supabase “Session” or “Direct” (5432)
# if the pooler cannot take locks — that URL is not what Vercel should use at runtime.

DATABASE_URL=

# ── Razorpay ────────────────────────────────────────────────────
# https://dashboard.razorpay.com/app/keys

RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Webhook signing secret (Dashboard → Webhooks), not the API Key Secret.
# https://dashboard.razorpay.com/app/webhooks

RAZORPAY_WEBHOOK_SECRET=
